/**
 * Placeholder for the generated Supabase database types.
 *
 * When the external Supabase project is connected, replace this with the
 * generated types from `supabase gen types typescript` so queries are fully
 * typed.
 */
export type Database = Record<string, unknown>;
